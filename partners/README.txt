PARTNER LOGOS

Only two partners are listed on the site. Drop logos here using these exact
filenames and they appear automatically:

  qiman.png
  aims.png

Until a file exists, that partner shows its name typeset instead, so the
section always looks intentional.

Specs: PNG, transparent background, around 600px wide, trimmed tight.

Do not add logos for organisations that have not given written permission.
